version: '3.8'

# Local dev override — DB only, run client/server natively
services:
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: product_template_dev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata_local:/var/lib/postgresql/data

volumes:
  pgdata_local:
